import ProductList from "../components/ProductList"

export const HomePage = () => {
    return (
        <main>
            <div className="flex">
                <ProductList />
            </div>
        </main>
    )
}
